#include <bits/stdc++.h>
using namespace std;

typedef pair<double, double> pdd;

int n;

int main()
{
	ios::sync_with_stdio(false);
	cin.tie(NULL);

	cin >> n;
	vector<pdd> v(n);

	for (int i = 0; i < n; i++)
	{
		cin >> v[i].first >> v[i].second;
	}

	// ½Å¹ß²ö °ø½Ä

}